import { useEntityIdentification } from 'src/stores/principal'

/**
 * Aqui se registrarán todas las Stores relacionadas con los formularios
 */
export const formStoreRegistry = {
  EntityIdentification: useEntityIdentification,
}
